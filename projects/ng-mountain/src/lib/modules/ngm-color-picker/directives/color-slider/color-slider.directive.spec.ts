import { ColorSliderDirective } from './color-slider.directive';

describe('ColorSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorSliderDirective();
    expect(directive).toBeTruthy();
  });
});
